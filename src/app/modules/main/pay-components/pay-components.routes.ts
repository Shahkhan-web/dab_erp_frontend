import { Route } from '@angular/router';
import { PayComponentsListComponent } from './pay-components-list.component';

export const payComponentsRoutes: Route[] = [{ path: '', component: PayComponentsListComponent }];
